# SKOOL Authentication Cookie
# Get this from your browser's developer tools after logging into SKOOL
# Look for the cookie named 'session' or similar
SKOOL_COOKIE=your_cookie_value_here

# Optional: Delay between requests in milliseconds
SCRAPE_DELAY=1500

# Use existing Chrome browser instead of launching new one
# Set to 'true' to connect to your existing Chrome session
# You must first launch Chrome with: /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222
USE_EXISTING_CHROME=false